# Generated documentation

This documentation is accessible via https://martchus.github.io/doc.

The `gh-pages` branch is so far simply updated by copying the generated documentation
generated and installed via the `sphinxdoc` and `install-doc` buildsystem targets, e.g.:

```
rsync -ahP /usr/share/syncthingtray-git/doc/ syncthingtray/
```

